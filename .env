# =============================================================================
# VALHEIM SERVER CONFIGURATION
# =============================================================================

# -----------------------------------------------------------------------------
# Basic Server Settings
# -----------------------------------------------------------------------------
SERVER_NAME=My Valheim Server
WORLD_NAME=Dedicated
SERVER_PASS=secret123
SERVER_PUBLIC=true
SERVER_PORT=2456

# Crossplay support (true/false)
CROSSPLAY=false

# World generation settings
# Leave empty for random seed
WORLD_SEED=""
# World size in km radius (default is random between 9000-10500)
# Recommended range: 100-20000
WORLD_SIZE=""

# -----------------------------------------------------------------------------
# Network Ports
# -----------------------------------------------------------------------------
# Game port (UDP)
GAME_PORT=2456
# Query port (UDP) - typically GAME_PORT + 1
QUERY_PORT=2457
# Crossplay port (UDP) - typically GAME_PORT + 2
CROSSPLAY_PORT=2458

# -----------------------------------------------------------------------------
# Update & Maintenance Settings
# -----------------------------------------------------------------------------
# Interval in seconds between update checks (default: 900 = 15 minutes)
UPDATE_INTERVAL=900

# -----------------------------------------------------------------------------
# Backup Settings
# -----------------------------------------------------------------------------
BACKUPS_ENABLED=true
# Interval in seconds between backups (default: 3600 = 1 hour)
BACKUPS_INTERVAL=3600
BACKUPS_DIRECTORY=/userfiles/backups
# Maximum age of backups in days
BACKUPS_MAX_AGE=3

# -----------------------------------------------------------------------------
# System Settings
# -----------------------------------------------------------------------------
TZ=America/New_York
PUID=1000
PGID=1000

# Memory limits (for docker-compose)
MEMORY_LIMIT=8G
MEMORY_RESERVATION=4G

# -----------------------------------------------------------------------------
# World Modifiers - Choose ONE preset OR set individual modifiers
# -----------------------------------------------------------------------------
# Presets: normal, casual, easy, hard, hardcore, immersive, hammer
# Leave empty to use individual modifiers below
PRESET=""

# Individual Modifiers (override preset values if PRESET is empty)
# Combat difficulty: veryeasy, easy, hard, veryhard
MODIFIER_COMBAT=""

# Death penalty: casual, veryeasy, easy, hard, hardcore
MODIFIER_DEATHPENALTY=""

# Resource availability: muchless, less, more, muchmore, most
MODIFIER_RESOURCES=""

# Raid frequency: none, muchless, less, more, muchmore
MODIFIER_RAIDS=""

# Portal restrictions: casual, hard, veryhard
MODIFIER_PORTALS=""

# -----------------------------------------------------------------------------
# Special Game Modifiers (true/false)
# -----------------------------------------------------------------------------
# No building material cost
SETKEY_NOBUILDCOST=false

# Individual player raid progression
SETKEY_PLAYEREVENTS=false

# Enemies won't attack unless provoked
SETKEY_PASSIVEMOBS=false

# Disable map/minimap
SETKEY_NOMAP=false

# -----------------------------------------------------------------------------
# Advanced Server Arguments
# -----------------------------------------------------------------------------
# Add any additional server arguments here that aren't covered above
# Examples:
#   -instanceid <number>          Set a specific instance ID
#   -nographics                   Run in headless mode (already enabled by default)
#   -batchmode                    Run in batch mode (already enabled by default)
#   -savedir <path>               Set custom save directory
#   -logFile <path>               Set custom log file path
# 
# NOTE: Do NOT add -crossplay, -public, -worldseed, or -worldsize here
#       These are controlled by the variables above
SERVER_ARGS=

# Example with instance ID:
# SERVER_ARGS=-instanceid 42

# -----------------------------------------------------------------------------
# BepInEx / Modding Settings (Optional)
# -----------------------------------------------------------------------------
# Set to true to enable BepInEx for modding support
BEPINEX_ENABLED=false

# -----------------------------------------------------------------------------
# Volume Paths (for docker-compose)
# -----------------------------------------------------------------------------
# Local paths for persistent data
USERFILES_PATH=./userfiles
SERVERFILES_PATH=./serverfiles